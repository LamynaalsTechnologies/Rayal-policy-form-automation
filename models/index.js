// Central export file for all Mongoose models
const RelianceJobQueue = require("./RelianceJobQueue");
const ProviderCredential = require("./ProviderCredential");

module.exports = {
  RelianceJobQueue,
  ProviderCredential,
};
